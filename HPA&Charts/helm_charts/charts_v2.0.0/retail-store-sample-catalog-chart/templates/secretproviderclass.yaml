{{- if and (eq .Values.app.persistence.provider "mysql") .Values.app.persistence.secret.useSecretsManager }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "catalog.fullname" . }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "catalog.labels" . | nindent 4 }}
spec:
  provider: aws
  parameters:
    region: {{ .Values.app.persistence.secret.secretsManager.region | default "us-east-1" }}
    usePodIdentity: "true"
    objects: |
      - objectName: {{ .Values.app.persistence.secret.secretsManager.secretName | quote }}
        objectType: "secretsmanager"
        jmesPath:
          - path: "username"
            objectAlias: "username"
          - path: "password"
            objectAlias: "password"
  
  secretObjects:
    - secretName: {{ .Values.app.persistence.secret.name }}
      type: Opaque
      data:
        - objectName: username
          key: RETAIL_CATALOG_PERSISTENCE_USER
        - objectName: password
          key: RETAIL_CATALOG_PERSISTENCE_PASSWORD
{{- end }}